- name: Check if folder exists
  stat:
    path: "{{ repo.dest }}"
  register: file_details

- name: Clone repos
  git:
    repo: "git@github.com:{{ repo.owner }}/{{ repo.repo }}"
    dest: "{{ repo.dest }}"
    version: master
  when: not file_details.stat.exists
